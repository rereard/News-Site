const Header = ({title}) => {
    return(
        <header className="md:text-3xl text-2xl p-3 border-b-2 w-full text-center font-medium">
            {title}
        </header>
    )
}
export default Header